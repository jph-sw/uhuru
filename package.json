{
  "name": "uhuru",
  "module": "index.ts",
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "prettier": "^3.8.1"
  },
  "scripts": {
    "dev": "bun run --filter './apps/*' --filter './packages/*' dev",
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
